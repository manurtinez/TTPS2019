package usuario;


import java.util.List;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import model.Admin;
import model.Dueno;
import model.Usuario;
import model.Veterinario;

public class TestUser {
/*
	//Usuario u = new Usuario("eva", "peron", "evap@gmail.com", "1234", 1234);
	Veterinario v = new Veterinario("america", "latina", "america@gmail.com", "1234", 1234, "asd", "asd", 12);
	Admin a = new Admin("alberto", "cristina", "hola@gmail.com", "1234", 1234);
	Dueno d = new Dueno("seba", "pose", "hola@gmail.com", "1234", 1234);
	//Dueno usuariojpa = new UsuarioDAOjpa();
	
	@Before
	public void setUp() throws Exception {
		
	}

	@Test
	public void testUser() {
		List<Usuario> usuarios = usuariojpa.getAll();
		Assert.assertEquals(3, usuarios.size());
		//usuariojpa.save(u);
		usuariojpa.save(v);
		usuarios = usuariojpa.getAll();
		Assert.assertEquals(5, usuarios.size());
		usuariojpa.delete(v);
		usuarios = usuariojpa.getAll();
		Assert.assertEquals(4, usuarios.size());
		//Assert.assertEquals(u.getApellido(), usuariojpa.getById(u.getId()).getApellido());
		//usuariojpa.delete(u);
		//Assert.assertEquals(usuariojpa.getById(u.getId()), null);
	}
*/
}
